/*
 * package com.ezen.book.controller;
 * 
 * import javax.inject.Inject;
 * 
 * import org.springframework.stereotype.Controller; import
 * org.springframework.web.bind.annotation.GetMapping; import
 * org.springframework.web.bind.annotation.PostMapping; import
 * org.springframework.web.bind.annotation.RequestMapping; import
 * org.springframework.web.servlet.mvc.support.RedirectAttributes;
 * 
 * import com.ezen.book.domain.MemberVO; import
 * com.ezen.book.service.MemberService;
 * 
 * import lombok.extern.slf4j.Slf4j;
 * 
 * @Slf4j
 * 
 * @RequestMapping("/mem/*")
 * 
 * @Controller public class MemberController {
 * 
 * @Inject private MemberService memberService;
 * 
 * @GetMapping("loginPage") public String loginPage() { return "/member/login";
 * }
 * 
 * @GetMapping("joinPage") public String joinPage() { return "/member/join"; }
 * 
 * @PostMapping("join") public String login(MemberVO mvo, RedirectAttributes
 * reAttr) { log.info(">>> member join check 1");
 * 
 * boolean isUp = memberService.join(mvo); return ""; } }
 */